@model Note
@{
    ViewData["Title"] = "发布笔记";
}


@using (Html.BeginForm())
{
    @Html.ValidationSummary(false);

    <div class="form-group">
        @Html.LabelFor(n => n.Title)
        @Html.TextBoxFor(n => n.Title, new { @class = "form-control", placeholder = "请输入标题..." })
    </div>
    <div class="form-group">
        @Html.LabelFor(n => n.Content)
        <textarea asp-for="Content" class="form-control" rows="5" placeholder="请输入内容..."></textarea>
    </div>
    <p>
        <button type="submit" class="btn btn-block btn-primary">发表</button>
    </p>
}

